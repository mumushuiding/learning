// =================
//  f0401 数学表达式
// =================
#include<iostream>
#include<cmath>
using namespace std;
int main(){
  double x;
  double a,c;
  sqrt(pow(sin(x),2.5));
  (a*x+(a+x)/(4*a))/2;
  (pow(c,pow(x,2)))/(sqrt(2*x));
  
}